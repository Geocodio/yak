[defaults]
inventory = ansible/inventory/hosts.yml
vault_password_file = ansible/vault/.vault_pass

[ssh_connection]
pipelining = true
ssh_args = -o ControlMaster=auto -o ControlPersist=600s -o PreferredAuthentications=publickey
